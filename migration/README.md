# Migration

This folder is reserved for migration bridges, legacy compatibility layers, quarantined prototypes, and transitional artifacts.

Rules:
- `immigrate-worker` and jobs-related utilities belong here, not in core production
- legacy chat prototypes stay here until formally removed
- duplicate files and historical artifacts should be quarantined here before deletion

Anything here should be assumed non-canonical unless explicitly promoted into `core/`.
