name = "admin-worker"
main = "src/index.js"
compatibility_date = "2026-01-27"

# Publish:
# https://admin-worker.malemodel-bkk.workers.dev

[vars]
ALLOWED_ORIGINS = "https://mmdprive.com,https://mmdprive.webflow.io"

AIRTABLE_BASE_ID = "appsV1ILPRfIjkaYg"

# Tables (ใส่ได้ทั้ง Table ID หรือ Table Name)
AIRTABLE_TABLE_MEMBERS = "tbl_cmjals5ud001e0svfhuamh6kn"
AIRTABLE_TABLE_SESSIONS = "sessions"
AIRTABLE_TABLE_PACKAGES = "packages"
AIRTABLE_TABLE_MEMBER_PACKAGES = "member_packages"
AIRTABLE_TABLE_PAYMENTS = "payments"
AIRTABLE_TABLE_POINTS_LEDGER = "points_ledger"

# Airtable Writer tables (ตามที่ล็อกไว้)
AIRTABLE_TABLE_CONSOLE_INBOX_ID = "tblFHmfpB2TTrzO2e"
AIRTABLE_TABLE_PAYMENT_PROOFS_ID = "tblfJfM4Sqag9zrLi"

# optional
AIRTABLE_TABLE_MODELS = "Models"

# telegram-worker internal send (optional)
TELEGRAM_INTERNAL_SEND_URL = "https://telegram-worker.malemodel-bkk.workers.dev/telegram/internal/send"

# payments-worker base (ถ้า admin-worker มี endpoint ที่ต้องยิงไป payments-worker)
PAYMENTS_WORKER_BASE = "https://payments-worker.malemodel-bkk.workers.dev"

# (optional) ถ้าอยากให้ admin-worker รู้ thread id ด้วย (ตอนนี้ใส่ไว้ได้ ไม่พัง)
TELEGRAM_CHAT_ID = "-1003546439681"
TG_THREAD_CONFIRM = "61"
TG_THREAD_POINTS = "17"
